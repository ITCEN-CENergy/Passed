name: Production Deploy

on:
  push:
    branches:
      - main
    paths:
      - "passed-frontend/**"
      - "passed-backend/**"
      - "passed-ai/**"
      - "infra/prod/**"
      - "infra/prometheus/**"
      - "infra/grafana/provisioning/**"
      - "infra/grafana/dashboards/**"
      - "infra/oracle/nginx.conf"
      - ".github/workflows/deploy-prod.yml"

  workflow_dispatch:

permissions:
  contents: read
  packages: write
  pull-requests: read

concurrency:
  group: passed-production
  cancel-in-progress: false

jobs:
  detect_dev_to_main_merge:
    name: Detect dev to main merge
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      run_embedding: ${{ steps.detect.outputs.result }}

    steps:
      - name: Detect merged dev pull request
        id: detect
        uses: actions/github-script@v7
        with:
          result-encoding: string
          script: |
            if (context.eventName !== 'push') {
              core.info('Embedding is disabled for non-push events.')
              return 'false'
            }

            const { data: pullRequests } =
              await github.rest.repos.listPullRequestsAssociatedWithCommit({
                owner: context.repo.owner,
                repo: context.repo.repo,
                commit_sha: context.sha,
              })

            const mergedDevPullRequest = pullRequests.find((pullRequest) =>
              pullRequest.merged_at !== null &&
              pullRequest.base.ref === 'main' &&
              pullRequest.head.ref === 'dev' &&
              pullRequest.merge_commit_sha === context.sha
            )

            if (!mergedDevPullRequest) {
              core.info('The current commit is not a merged dev -> main PR.')
              return 'false'
            }

            core.info(
              `Embedding enabled for PR #${mergedDevPullRequest.number}.`
            )
            return 'true'

  validate-compose:
    name: Validate Production Compose
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      IMAGE_TAG: ${{ github.sha }}
      TAILSCALE_IP: 127.0.0.1
      POSTGRES_DB: passed_validation
      POSTGRES_USER: passed_validation
      POSTGRES_PASSWORD: validation-only-password
      JWT_SECRET: validation-only-secret-that-is-long-enough
      OPENAI_API_KEY: validation-only-placeholder
      GRAFANA_ADMIN_USER: admin
      GRAFANA_ADMIN_PASSWORD: validation-only-password

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate production Compose
        run: docker compose -f infra/prod/compose.prod.yml config --quiet

  build-frontend:
    name: Build Frontend
    runs-on: ubuntu-latest
    timeout-minutes: 20

    defaults:
      run:
        working-directory: passed-frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: passed-frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build production frontend
        env:
          VITE_API_BASE_URL: ""
          VITE_AI_BASE_URL: ""
        run: npm run build

      - name: Upload frontend artifact
        uses: actions/upload-artifact@v4
        with:
          name: passed-frontend-${{ github.sha }}
          path: passed-frontend/dist
          if-no-files-found: error
          retention-days: 7

  build-images:
    name: Build ${{ matrix.service }}
    runs-on: ubuntu-latest
    timeout-minutes: 60

    strategy:
      fail-fast: false
      matrix:
        include:
          - service: backend
            context: ./passed-backend
            dockerfile: ./passed-backend/Dockerfile
            image: ghcr.io/itcen-cenergy/passed-backend
          - service: ai
            context: ./passed-ai
            dockerfile: ./passed-ai/Dockerfile
            image: ghcr.io/itcen-cenergy/passed-ai
          - service: job-posting-loader
            context: ./passed-ai
            dockerfile: ./passed-ai/Dockerfile.job-posting-loader
            image: ghcr.io/itcen-cenergy/passed-job-posting-loader

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64
          push: true
          tags: |
            ${{ matrix.image }}:${{ github.sha }}
            ${{ matrix.image }}:latest
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,mode=max,scope=${{ matrix.service }}

  deploy-home-server:
    name: Deploy Home Server
    needs:
      - validate-compose
      - build-images
    runs-on:
      - self-hosted
      - windows
    timeout-minutes: 60
    environment:
      name: production
    env:
      IMAGE_TAG: ${{ github.sha }}
      PROD_ENV_FILE: 'C:\srv\passed\.env.prod'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify production environment file
        shell: powershell
        run: |
          if (-not (Test-Path -LiteralPath $env:PROD_ENV_FILE)) {
            throw "Production environment file not found: $env:PROD_ENV_FILE"
          }

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Validate Compose configuration
        shell: powershell
        run: |
          docker compose `
            --project-name passed-prod `
            --env-file $env:PROD_ENV_FILE `
            -f infra/prod/compose.prod.yml `
            config --quiet

      - name: Initialize database and apply Flyway migrations
        shell: powershell
        run: |
          .\infra\prod\initialize-database.ps1 `
            -EnvironmentFile $env:PROD_ENV_FILE `
            -LoaderImage "ghcr.io/itcen-cenergy/passed-job-posting-loader:$env:IMAGE_TAG"

      - name: Pull production images
        shell: powershell
        run: |
          docker compose `
            --project-name passed-prod `
            --env-file $env:PROD_ENV_FILE `
            -f infra/prod/compose.prod.yml `
            pull

      - name: Deploy production stack
        shell: powershell
        run: |
          docker compose `
            --project-name passed-prod `
            --env-file $env:PROD_ENV_FILE `
            -f infra/prod/compose.prod.yml `
            up -d --remove-orphans

      - name: Verify production services
        shell: powershell
        run: |
          $compose = @(
            "compose",
            "--project-name", "passed-prod",
            "--env-file", $env:PROD_ENV_FILE,
            "-f", "infra/prod/compose.prod.yml"
          )

          function Wait-ForService {
            param(
              [string]$Service,
              [string[]]$HealthCommand
            )

            for ($attempt = 1; $attempt -le 20; $attempt++) {
              & docker @compose exec -T $Service @HealthCommand
              if ($LASTEXITCODE -eq 0) {
                Write-Host "$Service is healthy."
                return
              }

              Write-Host "Waiting for $Service ($attempt/20)"
              Start-Sleep -Seconds 6
            }

            throw "$Service health check failed."
          }

          Wait-ForService `
            -Service "postgres" `
            -HealthCommand @(
              "sh",
              "-c",
              'pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"'
            )

          Wait-ForService `
            -Service "ai" `
            -HealthCommand @(
              "curl",
              "--fail",
              "--silent",
              "http://localhost:8000/health"
            )

          Wait-ForService `
            -Service "backend" `
            -HealthCommand @(
              "wget",
              "-qO-",
              "http://localhost:8080/actuator/health"
            )

      - name: Show production status
        if: always()
        shell: powershell
        run: |
          docker compose `
            --project-name passed-prod `
            --env-file $env:PROD_ENV_FILE `
            -f infra/prod/compose.prod.yml `
            ps

      - name: Show production logs on failure
        if: failure()
        shell: powershell
        run: |
          docker compose `
            --project-name passed-prod `
            --env-file $env:PROD_ENV_FILE `
            -f infra/prod/compose.prod.yml `
            logs --tail 200

  deploy-oci:
    name: Deploy Frontend and Nginx to OCI
    needs:
      - build-frontend
      - deploy-home-server
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment:
      name: production
    env:
      OCI_HOST: ${{ secrets.OCI_HOST }}
      OCI_USER: ${{ secrets.OCI_USER }}
      OCI_SSH_KEY: ${{ secrets.OCI_SSH_KEY }}
      OCI_KNOWN_HOSTS: ${{ secrets.OCI_KNOWN_HOSTS }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download frontend artifact
        uses: actions/download-artifact@v4
        with:
          name: passed-frontend-${{ github.sha }}
          path: frontend-dist

      - name: Package frontend artifact
        shell: bash
        run: tar -C frontend-dist -czf passed-frontend.tar.gz .

      - name: Configure SSH
        shell: bash
        run: |
          set -Eeuo pipefail
          install -d -m 700 "${HOME}/.ssh"
          printf '%s' "${OCI_SSH_KEY}" > "${HOME}/.ssh/id_ed25519"
          printf '%s\n' "${OCI_KNOWN_HOSTS}" > "${HOME}/.ssh/known_hosts"
          chmod 600 "${HOME}/.ssh/id_ed25519" "${HOME}/.ssh/known_hosts"

      - name: Upload frontend and Nginx configuration
        shell: bash
        run: |
          set -Eeuo pipefail
          scp passed-frontend.tar.gz \
            "${OCI_USER}@${OCI_HOST}:/tmp/passed-frontend.tar.gz"
          scp infra/oracle/nginx.conf \
            "${OCI_USER}@${OCI_HOST}:/tmp/passed.conf"

      - name: Activate OCI release
        shell: bash
        run: |
          set -Eeuo pipefail
          ssh "${OCI_USER}@${OCI_HOST}" \
            "RELEASE_SHA='${{ github.sha }}' bash -s" <<'REMOTE'
          set -Eeuo pipefail

          release_root="/var/www/passed/releases"
          release_dir="${release_root}/${RELEASE_SHA}"
          current_link="/var/www/passed/current"
          temporary_release="/tmp/passed-frontend-${RELEASE_SHA}"
          previous_release="$(readlink -f "${current_link}" 2>/dev/null || true)"
          nginx_config="/etc/nginx/sites-available/passed.conf"
          nginx_backup="/tmp/passed.conf.backup"

          rm -rf "${temporary_release}"
          mkdir -p "${temporary_release}"
          tar -xzf /tmp/passed-frontend.tar.gz -C "${temporary_release}"

          sudo install -d -m 755 "${release_root}"
          sudo rm -rf "${release_dir}"
          sudo mv "${temporary_release}" "${release_dir}"
          sudo chown -R root:root "${release_dir}"
          sudo ln -sfn "${release_dir}" "${current_link}"

          if sudo test -f "${nginx_config}"; then
            sudo cp "${nginx_config}" "${nginx_backup}"
          else
            sudo rm -f "${nginx_backup}"
          fi

          sudo install -m 644 /tmp/passed.conf "${nginx_config}"
          sudo ln -sfn "${nginx_config}" \
            /etc/nginx/sites-enabled/passed.conf

          if ! sudo nginx -t; then
            if sudo test -f "${nginx_backup}"; then
              sudo cp "${nginx_backup}" "${nginx_config}"
            else
              sudo rm -f "${nginx_config}"
            fi

            if [ -n "${previous_release}" ]; then
              sudo ln -sfn "${previous_release}" "${current_link}"
            fi

            exit 1
          fi

          sudo systemctl reload nginx

          rm -f /tmp/passed-frontend.tar.gz /tmp/passed.conf
          sudo rm -f "${nginx_backup}"

          mapfile -t old_releases < <(
            sudo find "${release_root}" \
              -mindepth 1 \
              -maxdepth 1 \
              -type d \
              -printf '%T@ %p\n' |
              sort -rn |
              tail -n +6 |
              cut -d' ' -f2-
          )

          for old_release in "${old_releases[@]}"; do
            sudo rm -rf -- "${old_release}"
          done
          REMOTE

      - name: Verify OCI frontend
        shell: bash
        run: |
          set -Eeuo pipefail

          for attempt in $(seq 1 12); do
            if curl \
              --fail \
              --silent \
              --show-error \
              "http://${OCI_HOST}/nginx-health"; then
              exit 0
            fi

            echo "Waiting for OCI frontend (${attempt}/12)"
            sleep 5
          done

          exit 1

  embed-job-postings:
    name: Embed Production Job Postings
    needs:
      - detect_dev_to_main_merge
      - deploy-home-server
    if: >-
      needs.detect_dev_to_main_merge.outputs.run_embedding == 'true' &&
      needs['deploy-home-server'].result == 'success'
    runs-on:
      - self-hosted
      - windows
    timeout-minutes: 180
    environment:
      name: production
    env:
      IMAGE_TAG: ${{ github.sha }}
      PROD_ENV_FILE: 'C:\srv\passed\.env.prod'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify production environment file
        shell: powershell
        run: |
          if (-not (Test-Path -LiteralPath $env:PROD_ENV_FILE)) {
            throw "Production environment file not found: $env:PROD_ENV_FILE"
          }

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Run job-posting embeddings
        shell: powershell
        run: |
          .\infra\prod\run-job-posting-embedding.ps1 `
            -EnvironmentFile $env:PROD_ENV_FILE `
            -PipelineImage "ghcr.io/itcen-cenergy/passed-job-posting-loader:$env:IMAGE_TAG" `
            -BatchSize 100
